<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.mapper.MemberMapper">
	
	<select id="login" parameterType="map" resultType="board.dto.MemberDTO">
		select id,pass,name,age,(select lower(grade_name) from grade_list where grade_no = grade_name and id = #{id} and pass = #{pass}
	</select>
	
	
</mapper>